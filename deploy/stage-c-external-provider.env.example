# HTTPS endpoint in the independent alert/Page fault domain. The literal
# placeholder is replaced only with the verified registrar challenge ID.
STAGE_C_PROVIDER_RECEIPT_URL_TEMPLATE=https://alerts.example.invalid/v1/stage-c/receipts/{challenge_id}
